<h1>All Users</h1>
<table>
	<thead>
		<tr>
			<th> Name </th>
			<th>			</th>
			<th> Email </th>	
		</tr>
	</thead>

	<tbody>
		<% @users.each do |user| %>
			<tr>	
				<td> <%= link_to user.name, user_path(user) %> </td>
				<td>									 </td>
				<td>  <%= user.email %> </td>
				<td>									  </td>
        <td><%= link_to('Edit', edit_user_path(user)) %></td>
			</tr>
		<%end%>	
	</tbody>
</table>